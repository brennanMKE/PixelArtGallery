# 0079 — Display selection flips to the wrong FT when two displays share the same dimensions

| | |
|---|---|
| **Status** | in-progress |
| **Module** | UI / Models |
| **Platform** | All |
| **First seen** | 2026-07-11 |
| **Commit** | `001b2e2` |

## Description

With two FT displays both set to 45×35, opening an image sometimes preselects the *other* 45×35 display instead of the one the user has been using. Root cause: `FlaschenTaschenDisplay.preferredSelection(...)` breaks ties among same-dimension displays by **list order** (`matches.first`), not by the **last-used** display. Fix the selection logic so that when several displays match the variant's dimensions, it prefers the last-used display (then the current selection), rather than arbitrarily switching to another same-dimension FT.

## Long Description

`VariantDetailView.updateSelectedDisplay()` calls `FlaschenTaschenDisplay.preferredSelection(current:variantWidth:variantHeight:among:)`:

```swift
let matches = candidates.filter { $0.width == variantWidth && $0.height == variantHeight }
if !matches.isEmpty {
    if let current, matches.contains(where: { $0.id == current }) { return current }
    return matches.first?.id      // <-- arbitrary among same-dimension displays; ignores last-used
}
```

When the variant is 45×35 and two displays are 45×35, `matches` has both. If `current` (the screen's `selectedDisplayID`) isn't already set to one of them — which is the case on a fresh open of the screen — it returns `matches.first?.id`, i.e. whichever display the `@Query` happens to order first. It never consults the persisted last-used display. So the selection "changes to the other one." The popover (`GallerySendPopoverView`) already defaults correctly via `GalleryCoordinator.resolveLastUsedDisplay(among:)` (which returns the stored last-used id when present), but `preferredSelection` — the path `VariantDetailView` uses — does not.

## Root cause

Confirmed exactly as filed. `FlaschenTaschenDisplay.preferredSelection(current:variantWidth:variantHeight:among:)` (`Models/FlaschenTaschenDisplay.swift:158-177`, pre-fix) filtered candidates to dimension `matches`, kept `current` if it was among them, and otherwise returned `matches.first?.id` — the name-sorted `@Query` order — with no consultation of the persisted last-used display. On a fresh open of `VariantDetailView`, `selectedDisplayID` starts `nil`, so with two 45×35 displays the picker landed on whichever sorted first by `displayName`, flipping away from whichever display the user had actually been using. Separately, `VariantDetailView`'s `onChange(of: selectedDisplayID)` only called `seedSendDefaults()` — unlike `GallerySendPopoverView`, it never called `coordinator.rememberLastUsedDisplay(_:)`, so picking a display on this screen didn't keep the persisted last-used value current either.

## Fix

Followed the plan as written, no divergence:

1. **`FlaschenTaschenDisplay.preferredSelection`** (`Models/FlaschenTaschenDisplay.swift`) gained a defaulted `lastUsed: UUID? = nil` parameter (positioned right after `current`). Among the dimension `matches`, the tie-break order is now: (1) `current` if it's a match (unchanged), (2) `lastUsed` if it's a match (new — one line inserted between the existing `current` check and the `matches.first` return), (3) `matches.first?.id` (unchanged, final fallback). The no-matches fallback ladder (`current` if still valid → `source == defaultSource` → `candidates.first` → `nil`) is byte-for-byte unchanged; `lastUsed` does not participate there, per the plan. Updated the doc comment to describe the new tie-break, and fixed the two stale DocC references to the old signature: `GalleryCoordinator.swift:1151` and the header comment of `DisplayPreferredSelectionTests.swift`.
2. **`VariantDetailView.updateSelectedDisplay()`** now passes `lastUsed: coordinator.resolveLastUsedDisplay(among: displays)?.id` into the `preferredSelection` call. The candidate-tuple building is otherwise unchanged, and the `pendingCenteredDisplayID` early return (the #0064 display-first push) still executes *before* `preferredSelection` is ever called, so centered-seed precedence is preserved untouched.
3. **`VariantDetailView`'s `onChange(of: selectedDisplayID)`** gained a `newValue` parameter and now also calls `coordinator.rememberLastUsedDisplay(display)` when the new id resolves to a display, mirroring `GallerySendPopoverView.swift:125-128`. This fires for programmatic selection too (auto-match on open, the centered-seed assignment) exactly as the popover's does — idempotent, and doesn't fight `seedSendDefaults()`/`SendDefaultsSeed` since remembering only writes a `UserDefaults` string.
4. **`GalleryCoordinator` and `GallerySendPopoverView`** were not changed beyond the one DocC signature fix in `GalleryCoordinator.swift:1151` — both were already correct per the plan's analysis.

## Verification

1. `cd PixelArtGalleryKit && swift test` — full suite passed: **235 tests in 29 suites**, up from 229 (6 new tests added), 0 failures. All new `DisplayPreferredSelectionTests` cases executed and passed by name: `lastUsedBreaksTieAmongEqualDimensionMatches`, `currentMatchingSelectionWinsOverLastUsed`, `lastUsedNotAmongMatchesFallsBackToFirstMatch`, `lastUsedDoesNotAffectNoMatchFallback`, `singleMatchIgnoresLastUsed`, `nilLastUsedKeepsFirstMatchOrder`. All twelve pre-existing `DisplayPreferredSelectionTests` cases (including `picksFirstMatchInCandidateOrderWhenCurrentDoesNotMatch`, which pins the `lastUsed: nil` compatibility path) still passed unchanged.
2. `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=macOS' build` — **BUILD SUCCEEDED**.
3. `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` — **BUILD SUCCEEDED**.
4. **Device-pending:** the full two-45×35-displays interaction in the running app (select B, leave, reopen, confirm it stays on B; repeat favoring A; confirm a #0064 display-first push still lands on the pushed display with its centered offset) requires tapping through `VariantDetailView` on a device/simulator with two displays configured and was not run in this pass. The underlying tie-break logic (`preferredSelection`'s `lastUsed` parameter) that this interaction depends on is fully covered by the unit tests above, including the exact "two matches, `lastUsed` is the second" scenario from the bug report.

## Files changed

- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Models/FlaschenTaschenDisplay.swift` — added `lastUsed` parameter and tie-break to `preferredSelection`; updated doc comment.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift` — pass `lastUsed` in `updateSelectedDisplay()`; extend `onChange(of: selectedDisplayID)` to remember the selection.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/ViewModels/GalleryCoordinator.swift` — fixed stale DocC reference to the `preferredSelection` signature at `resolveLastUsedDisplay(among:)`.
- `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/UI/DisplayPreferredSelectionTests.swift` — updated header DocC reference; added six `#0079` test cases.

## Plan

Root cause is confirmed exactly as described: `FlaschenTaschenDisplay.preferredSelection` (`Models/FlaschenTaschenDisplay.swift:158-177`) filters `candidates` to dimension `matches`, keeps `current` if it's among them, and otherwise returns `matches.first?.id` — the name-sorted `@Query` order — never consulting the persisted last-used display (#0066). On a fresh open of `VariantDetailView`, `selectedDisplayID` is `nil`, so with two 45×35 displays the picker lands on whichever sorts first, flipping away from the display the user has been using. Separately, `VariantDetailView` never calls `coordinator.rememberLastUsedDisplay(_:)` — its `onChange(of: selectedDisplayID)` (line 318) only calls `seedSendDefaults()` — so picking a display on this screen doesn't update last-used the way the popover does (`GallerySendPopoverView.swift:125-128`).

### 1. Extend `preferredSelection` with a `lastUsed` tie-breaker

`PixelArtGalleryKit/Sources/PixelArtGalleryKit/Models/FlaschenTaschenDisplay.swift` — change the signature to:

```swift
static func preferredSelection(
    current: UUID?,
    lastUsed: UUID? = nil,
    variantWidth: Int,
    variantHeight: Int,
    among candidates: [(id: UUID, source: String, width: Int, height: Int)]
) -> UUID?
```

`lastUsed` is defaulted to `nil` so every existing call site (the one production caller in `VariantDetailView` and all twelve existing tests) compiles unchanged with today's behavior. New tie-break order **among `matches` only**:

1. `current`, if it is one of the matches — a deliberate, still-valid selection is never moved (unchanged from today);
2. `lastUsed`, if it is one of the matches — **new**: a fresh open (where `current` is `nil` or stale) now prefers the display the user last used over arbitrary list order;
3. `matches.first?.id` — unchanged final fallback (deterministic name-sorted order).

Concretely, insert one line between the existing `current` check and the `matches.first` return:

```swift
if let lastUsed, matches.contains(where: { $0.id == lastUsed }) {
    return lastUsed
}
```

The no-matches fallback ladder (`current` if still valid → `source == defaultSource` → `candidates.first` → `nil`) stays byte-for-byte unchanged — `lastUsed` deliberately does not participate there (that path already has the sensible #0032 semantics, and the popover's `resolveLastUsedDisplay` covers the last-used-without-geometry case). Keep it a pure static func operating on plain tuples (unit-testable without SwiftData). It is *not* marked `nonisolated` today — the package default-isolates `@MainActor` and the test suite is `@MainActor` — so leave isolation as-is; no change needed. Update the function's doc comment to describe the new tie-break, and fix the two DocC references to the old signature (`GalleryCoordinator.swift:1151` and the header comment of `DisplayPreferredSelectionTests.swift`).

### 2. Wire `VariantDetailView` — pass `lastUsed`, remember on change

`PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift`:

- **Pass the tie-breaker** in `updateSelectedDisplay()` (line 352): add `lastUsed: coordinator.resolveLastUsedDisplay(among: displays)?.id` to the `preferredSelection` call. Candidate-tuple building from the `@Query` is otherwise unchanged. Note: `resolveLastUsedDisplay` has its own fallback ladder (stored id → default-source → first), so when nothing is stored yet `lastUsed` resolves to the seeded default display — which means the tie among equal-dimension displays then breaks toward the built-in default rather than raw sort order. That is deterministic, matches the #0032 spirit, and is acceptable; no need for a raw-stored-id accessor.
- **Centered-seed precedence is preserved for free**: `updateSelectedDisplay()` returns early on `pendingCenteredDisplayID` (lines 353–356) *before* ever calling `preferredSelection`, so the #0064 display-first push still wins whenever it's pending. Confirm this ordering is untouched.
- **Add remember-on-change** (currently missing): extend the existing `onChange(of: selectedDisplayID)` (line 318) to also persist the selection, mirroring `GallerySendPopoverView.swift:125-128`:

  ```swift
  .onChange(of: selectedDisplayID) { _, newValue in
      seedSendDefaults()
      if let newValue, let display = displays.first(where: { $0.id == newValue }) {
          coordinator.rememberLastUsedDisplay(display)
      }
  }
  ```

  This fires for programmatic selection too (auto-match on open, the centered-seed assignment), exactly as the popover's does for its own programmatic init — that's consistent and idempotent: on a fresh open the auto-selection now *is* the last-used (or the sole dimension match, which is then a reasonable new last-used), and a #0064 push remembers the display the user explicitly chose in the display-first picker. No fight with `seedSendDefaults()`/`SendDefaultsSeed` — remembering only writes a UserDefaults string and reads no seed state.

### 3. Coordinator and popover — no changes

- `GalleryCoordinator.rememberLastUsedDisplay(_:)` / `resolveLastUsedDisplay(among:)` (`ViewModels/GalleryCoordinator.swift:1146-1168`) are reused as-is; only the DocC comment at line 1151 needs the updated `preferredSelection` signature.
- `GallerySendPopoverView` already defaults via `resolveLastUsedDisplay` (line 265) and remembers on change (line 127) — verified correct; **no change needed** (it never calls `preferredSelection`, so the bug cannot reproduce there).

### 4. Tests (Swift Testing — extend `DisplayPreferredSelectionTests`)

Extend `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/UI/DisplayPreferredSelectionTests.swift` (existing `@MainActor @Suite`; do not create a new file). Existing tests compile unchanged thanks to the defaulted parameter and pin the `lastUsed: nil` → old-behavior path (notably `picksFirstMatchInCandidateOrderWhenCurrentDoesNotMatch`). Add a `// MARK: - #0079 last-used tie-break` section:

- `lastUsedBreaksTieAmongEqualDimensionMatches` — two 45×35 candidates, `current: nil`, `lastUsed:` the **second** → returns the second, not `matches.first` (the core bug).
- `currentMatchingSelectionWinsOverLastUsed` — both candidates match, `current:` first, `lastUsed:` second → returns first (`current` still wins).
- `lastUsedNotAmongMatchesFallsBackToFirstMatch` — two matches, `lastUsed:` the id of a third, non-matching (or absent) display → `matches.first`.
- `lastUsedDoesNotAffectNoMatchFallback` — no candidate matches the variant dimensions, `lastUsed:` set to a candidate → still the default-source display (unchanged #0032 ladder).
- `singleMatchIgnoresLastUsed` — one matching display, `lastUsed:` a non-matching display's id → the sole match.
- `nilLastUsedKeepsFirstMatchOrder` — explicit `lastUsed: nil` with two matches → `matches.first` (locks the compatibility default).

### 5. Verification

1. `cd PixelArtGalleryKit && swift test` — confirm the new tests are listed in the output and pass (all existing `DisplayPreferredSelectionTests` too; "0 tests run" is a failure).
2. `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=macOS' build`
3. `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build`
4. **Run-the-app check (user/device):** with two 45×35 displays configured, select display B in a variant's Send picker (or send to B), leave, reopen an image/variant → the picker stays on B, not A; repeat favoring A. Also confirm a #0064 display-first push still lands on the pushed display with the centered offset.

## Notes

Relevant code:
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Models/FlaschenTaschenDisplay.swift` — `static func preferredSelection(current:variantWidth:variantHeight:among:)` (~line 158). The tie-break `matches.first?.id` is the defect.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift` — `updateSelectedDisplay()` (~line 352) is the caller; `selectedDisplayID`; the `onChange(of: selectedDisplayID)` chain. Verify whether it calls `coordinator.rememberLastUsedDisplay(_:)` when the user changes the display — if not, add it so "last used" stays current from this screen too (the popover does this at `GallerySendPopoverView.swift:127`).
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/ViewModels/GalleryCoordinator.swift` — `resolveLastUsedDisplay(among:)` / `rememberLastUsedDisplay(_:)` / `lastUsedDisplayIDKey` (#0066): the persisted last-used display. `resolveLastUsedDisplay` returns the stored id's display when it's among candidates (correct); reuse its result as the tie-breaker.

Design points (planner to finalize):
- **Tie-break toward last-used** in `preferredSelection`: add a `lastUsed: UUID?` parameter and, among the dimension `matches`, prefer in order: (1) `current` if it's one of the matches; (2) `lastUsed` if it's one of the matches; (3) `matches.first`. This keeps the "don't move a deliberate current selection" behavior while stopping the arbitrary flip between equal-dimension displays. Keep `preferredSelection` pure/testable (it already takes a plain tuple list — extend the signature).
- **Wire the caller**: `VariantDetailView.updateSelectedDisplay()` passes the last-used id (e.g. `coordinator.resolveLastUsedDisplay(among: displays)?.id`, or a direct read of the stored id) as `lastUsed`. Confirm the display `@Query`/candidate tuple building is unchanged otherwise.
- **Remember from VariantDetailView too**: if the send screen doesn't already `rememberLastUsedDisplay` when the user changes the picker, add it (so choosing display A here makes A the last-used everywhere). Confirm this doesn't fight the `pendingCenteredDisplayID`/seed logic (#0064) — the centered-seed path sets `selectedDisplayID` explicitly and should still win when present.
- Consider whether the popover path needs any change — it uses `resolveLastUsedDisplay` and should already be correct; verify and note. If the bug also reproduces in the popover, extend the fix there.
- No persistence/schema change (last-used already persists via #0066).

Testing (Swift Testing): extend the `preferredSelection` tests (or add) — two same-dimension candidates + a `lastUsed` equal to the second → returns the second (not the first); `current` set to a match still wins over `lastUsed`; `lastUsed` not among matches → falls back to `matches.first`; no matches → existing default-source/first fallback unchanged; single matching display → unchanged. **Verify by running the app**: with two 45×35 displays, select display B, leave and reopen an image/variant → it stays on B (not A); repeat favoring A. (Device/user check for the full flow.)

## Relation

- Follows: [#0066](0066.md) (last-used display persistence) and [#0064](0064.md)/[#0057](0057.md) (the send-screen selection + centered seed). Fixes the `preferredSelection` tie-break.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-07-11 | plan | claude-fable-5 | 7,486 | 429 | 165,641 | 54,431 | $0.94 |
| 2026-07-11 | implement | claude-sonnet-5 | 42 | 3,628 | 1,135,015 | 75,346 | $0.45 |

**Total: $1.39**
