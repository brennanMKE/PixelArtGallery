# 0067 — Popover UI (iOS & macOS): FT dropdown (default last-used), fitted+centered image, Send, saved-variants list

| | |
|---|---|
| **Status** | resolved |
| **Module** | UI |
| **Platform** | All |
| **First seen** | 2026-07-11 |
| **Commit** | 5280d15 |
| **Closed** | 2026-07-11 |

## Description

The popover itself ([#0065](0065.md)). Tapping a gallery image opens a **popover** (replacing the pushed item screen) containing exactly: an **FT display dropdown** defaulting to the **last-used** display, the **fitted + centered image** for the selected display, a **Send** button, and the **list of saved variants**. Selecting a display shows the transient fitted preview from [#0066](0066.md) (no distortion for any aspect); Send transmits that fitted grid centered on the display; saving promotes the transient preview into the saved-variants list. Shared for iOS and macOS.

## Long Description

Current navigation: `GalleryListView` (grid) → pushed `GalleryDetailView` → pushed `VariantDetailView`. This issue changes the grid cell tap to present a **popover** instead of pushing `GalleryDetailView`, and puts the whole send experience in that popover. Advanced per-frame layer/x/y controls are NOT in the popover (the offset is computed/centered automatically, layer uses the display's stored default); the existing `VariantDetailView` can remain as the editor reached from a saved-variant row.

## Plan

> API names from #0066 (`FittedPreview`, `fittedPreview(for:display:)`, `saveVariant(from:)`, the last-used-display resolver) are assumed below; track whatever #0066 actually lands.

### 1. Popover presentation — per-cell `.popover(item:)`, accept the iPhone sheet adaptation

In `GalleryListView.swift`, replace each cell's `NavigationLink(value: item)` with a `Button { selectedItem = item }` and attach the popover **to the cell**, not the grid. A single `.popover(item: $selectedItem)` cannot be attached to every cell (each visible cell would present when the binding goes non-nil), and attaching it once at the ScrollView level loses per-cell anchoring on macOS/iPad. So each cell gets a derived binding:

```swift
.popover(item: Binding(
    get: { selectedItem?.id == item.id ? selectedItem : nil },
    set: { if $0 == nil { selectedItem = nil } }
)) { item in
    GallerySendPopoverView(item: item, coordinator: coordinator)
}
```

- **iPhone (compact width): accept the default sheet adaptation.** Do NOT force `.presentationCompactAdaptation(.popover)` — this popover is a full send surface (dropdown + preview + Send + variants list); a true anchored popover on an iPhone is a cramped bubble that would clip the list and fight the keyboard. The automatic sheet is exactly what Apple's adaptation is for. Add `.presentationDragIndicator(.visible)`; no detents needed (the adapted sheet's default full presentation suits the list). Document the choice in a comment.
- **iPad / macOS**: real popover anchored on the tapped cell. On macOS give the content `.frame(minWidth: 360, idealWidth: 400, minHeight: 520, idealHeight: 620)` inside `#if os(macOS)` so it doesn't collapse to intrinsic size.
- **Dismissal**: outside-tap/ESC (macOS), swipe-down (iOS). The popover content's `.onDisappear` calls the send controller's `stop()` (see §3) so dismissing mid-send stops the loop and clears the display — the stop-clear already runs in a detached task (#0052/#0053), so it survives view teardown.
- Remove `GalleryListView`'s `.navigationDestination(for: GalleryItem.self)` — nothing routes to `GalleryDetailView` anymore (the file itself is retired in #0068, not here). Keep the root `NavigationStack` (toolbar/title still need it).

### 2. Popover content — new `UI/GallerySendPopoverView.swift`

One shared view for both platforms, wrapped in its **own `NavigationStack`** so a saved-variant row can push the existing `VariantDetailView` editor *inside* the popover/sheet (`VariantDetailView` already tears down its own send `.onDisappear`, and its `.navigationDestination(for: Variant.self)` registration moves here). Layout top-to-bottom:

1. **Header**: item name (`.navigationTitle` inline) — plus, on iOS, a Done/close button.
2. **FT display dropdown**: `@Query(sort: \FlaschenTaschenDisplay.displayName) displays`, a `Picker` (`.menu` style on macOS/iOS) bound to `@State selectedDisplayID: UUID?`. On appear (`onChange(of: displays…, initial: true)` like `VariantDetailView`), initialize the selection via #0066's last-used resolver (falling back per its rules); on user change, write the new id back through the #0066 last-used setter. Locked (`.disabled(isSending)`) during a send — same endpoint-captured-at-start rationale as #0057.
3. **Fitted preview**: `.task(id: selectedDisplayID)` calls `try await coordinator.fittedPreview(for: item, display:)` into `@State preview: FittedPreview?`, showing a `ProgressView` while nil/in-flight (the #0066 cache makes re-selection instant). Render inside a **display-canvas letterbox** (in — it's cheap and conveys placement): `Color.black.aspectRatio(displayW/displayH, contentMode: .fit)` with the fitted image centered in an `.overlay` — since the offset is always the centering offset, plain centering inside the display-aspect frame is visually exact.
4. **Send/Stop button**: borderedProminent, `Send Now` / red `Stop Sending`, disabled when `preview == nil`. Drives the shared send controller (§3) with the transient preview. Status banners (`StatusBanner`) as in `VariantDetailView`. **No layer/x/y steppers** — offset comes from the preview, layer from `display.layer`.
5. **Save button + saved-variants list**: a `Save as Variant` button calls `try coordinator.saveVariant(from: preview)` (disabled when nil; #0066 dedups repeats); the list shows `item.variants` (live via `@Model` observation) using the existing row style (`VariantThumbnailView` + dims), each row a `NavigationLink(value: variant)` → `VariantDetailView`. Nothing else ever persists the transient preview — mere selection/Send never accumulates variants.

Rendering the transient grid: extract `VariantThumbnailView`'s render path into a lightweight `PixelDataImageView(pixelGridData:width:height:)` (same `PixelGrid.fromRGBA8888(...).makeCGImage()` off-main + `Image(...).interpolation(.none).resizable().aspectRatio(contentMode: .fit)`), and make `VariantThumbnailView` a thin wrapper over it. Do **not** use `PixelGridView` here — its zoom/selection/readout chrome is wrong for a popover; no `PixelGridViewModel` change needed.

### 3. Send-loop reuse — extract `ViewModels/FTSendController.swift`

Move `VariantDetailView`'s continuous-send loop (`handleSendToDisplay`'s `sendTask` body, `stopSending`, `activeSend`, the 2s `sendRefreshInterval`, mid-send `requiresClear` clear, cancellation handling) into an `@Observable final class FTSendController` (MainActor via package default):

```swift
nonisolated struct FTSendPayload: Sendable {   // Networking/FTSendPayload.swift
    let host: String; let port: Int
    let width: Int; let height: Int
    let pixelGridData: Data; let scaleFactor: Double
}

@Observable final class FTSendController {
    private(set) var isSending = false
    private var sendTask: Task<Void, Never>?
    private var activeSend: FTPaintTarget?
    func start(payload: FTSendPayload,
               frameOffset: @escaping @MainActor () -> (layer: Int, offsetX: Int, offsetY: Int),
               onError: @escaping @MainActor (String) -> Void,
               onStopped: @escaping @MainActor (String) -> Void)
    func stop()   // cancel + detached sendClearFrame(to: activeSend) — #0053 semantics
}
```

`frameOffset` is read **each frame**, preserving #0057's live layer/x/y for `VariantDetailView` (its closure reads the `@State` steppers) while the popover passes a constant closure `{ (FlaschenTaschenDisplay.clampedLayer(display.layer), preview.offsetX, preview.offsetY) }`. Refactor `VariantDetailView` to delegate to the controller with identical behavior (banners stay in the view via the callbacks; `.onDisappear { controller.stop() }` unchanged). No duplication of the loop.

Pure, testable assembly helper (`Networking/FTSendPlan.swift`, `nonisolated`): `FTSendPlan.make(preview: FittedPreview, host:port:layer:) -> (payload: FTSendPayload, offset: (x: Int, y: Int, z: Int))` — clamps the layer via `FlaschenTaschenDisplay.clampedLayer`, offsets via `clampedOffset`, and carries the preview's fit dims/grid verbatim.

### 4. Empty state — no dead popover

When `displays` is empty, the popover body shows the Scan / Add Manually affordances instead of the picker+preview: extract `DisplayRegistryView`'s private `emptyState` + scan action into a small shared `UI/DisplayEmptyStateView` (taking the coordinator; presenting `DisplayEditorView(mode: .add)` in a sheet), used by both `DisplayRegistryView` and the popover. Note `GalleryListView` already seeds the default display (#0021), so this is a rare path — but it must work.

### 5. Platform notes

Single shared `GallerySendPopoverView`; `#if os(...)` only for: macOS popover `.frame`, iOS `.presentationDragIndicator`/Done button, and the existing navigation-bar conditionals. Both platforms must build **and be run** (see §6). iPad gets the true popover for free via regular width.

### 6. Testing + verification

**Swift Testing** (package, `cd PixelArtGalleryKit && swift test`):
- `FTSendPlanTests` — payload/offset assembly from a `FittedPreview` (fit dims carried, offsets = preview centering offsets, z = clamped `display.layer`, out-of-range layer clamped into 1…15).
- Controller-adjacent pure logic only; the loop itself is network-bound (existing #0057 behavior is regression-covered by `VariantDetailView` continuing to work).
- Default-display resolution is #0066's tests; here only assert the popover's wiring inputs via the plan helper if any glue logic is added.

**Builds (both platforms, mandatory):**
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=macOS' build`
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build`

**Run the app — mandatory, not optional** (#0064 shipped green-but-wrong because it was never run). On macOS and the iOS simulator, confirm by observation:
1. Tapping a gallery cell opens the popover (macOS: anchored popover; iPhone: sheet) — no push to `GalleryDetailView`.
2. The dropdown defaults to the last-used display and remembers a change across reopen.
3. Selecting a display with a non-matching aspect shows a visibly **un-stretched** fitted preview, letterboxed/centered in the display-aspect canvas.
4. Send toggles to Stop; dismissing the popover mid-send stops it (watch the log for the stop/clear lines).
5. Save adds exactly one row to the saved-variants list (and repeat-Save doesn't duplicate); merely selecting displays adds nothing.
6. A saved-variant row pushes the `VariantDetailView` editor inside the popover.

### Files changed (planned)

- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/GallerySendPopoverView.swift` — new popover content view.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/ViewModels/FTSendController.swift` — new; loop extracted from `VariantDetailView`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Networking/FTSendPayload.swift` / `FTSendPlan.swift` — new nonisolated value types + assembly helper.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/GalleryListView.swift` — cell Button + per-cell `.popover`, drop the `GalleryItem` navigationDestination.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift` — delegate send loop to `FTSendController` (behavior unchanged).
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantThumbnailView.swift` — factor out `PixelDataImageView` for raw grid data.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/DisplayRegistryView.swift` — extract shared `DisplayEmptyStateView`.
- `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/Networking/FTSendPlanTests.swift` — new Swift Testing suite.

## Resolution notes

> 🟢 Resolved 2026-07-11 — Popover flow lands cleanly: `FTSendController` is a faithful extraction of `VariantDetailView`'s continuous-send loop (the per-frame `frameOffset()` closure reads the live `sendLayer`/`sendOffsetX`/`sendOffsetY` @State each iteration, so #0057's mid-send steppers still take effect; the #0057 `requiresClear` mid-send clear and #0053 detached stop-clear are preserved; `isSending` is forwarded via a computed property). `FTSendPlan.make` computes `x = clampedOffset(preview.offsetX) + clampedOffset(display.offsetX)` (and y likewise), `z = clampedLayer(display.layer)`, matching #0066's `FittedPreview` centering-plus-stored-offset contract — a zero-offset display lands at the pure centering offset (centered) — and the 6 `FTSendPlanTests` assert exactly this. Popover wiring is sound (per-cell derived `.popover(item:)`, iPhone sheet adaptation without a forced `.presentationCompactAdaptation(.popover)`, macOS `.frame`, last-used-default dropdown, `.task(id:)` fitted preview with spinner, letterboxed un-stretched `PixelDataImageView`, Save/Send/variant-row push, `DisplayEmptyStateView` empty state, `.onDisappear` stop); the `GalleryItem` navigationDestination is removed while the root `NavigationStack` and `GalleryDetailView.swift` are kept. Independently verified: `swift test` = 179 tests / 24 suites pass (the 6 new `FTSendPlanTests` plus `SendDefaultsSeedTests`/`FTPaintTargetTests` observed by name), both macOS and iOS-simulator builds SUCCEEDED with no "unable to type-check in reasonable time" error and no pathological slowness (the `galleryCellButton(for:)` factoring holds). Scope is clean — only the intended UI/ViewModels/Networking files + tests changed, no SwiftData schema change, retired screens left in place for #0068. **Live in-app popover verification (tap opens popover, dropdown defaults/remembers, preview visibly un-stretched, Send/Stop + dismiss-mid-send clear, Save adds exactly one row, variant-row push) remains a pending user/orchestrator-run gate — it is not part of this code review.**

## Root cause

Not a bug fix — a feature gap. `GalleryListView` routed every grid-cell tap to the pushed `GalleryDetailView` → `VariantDetailView` chain; there was no popover surface combining the FT display dropdown, the #0066 transient fitted preview, Send, and the saved-variants list in one place, and no shared abstraction for the continuous-send loop other than the copy living inside `VariantDetailView` (#0057), so a second call site (the popover, sending a transient `FittedPreview` instead of a persisted `Variant`) would have had to duplicate it.

## Fix

- **`ViewModels/FTSendController.swift`** (new): `@Observable final class` holding the continuous-send loop verbatim-extracted from `VariantDetailView`'s old `handleSendToDisplay`/`stopSending` — `start(payload:frameOffset:onError:onStopped:)` and `stop()`, preserving the #0053 stop-clear (detached `sendClearFrame`), the #0057 mid-send clear-on-layer/offset-change, and cancellation handling. `frameOffset` is invoked once per frame so callers can keep layer/x/y live-editable during a send (`VariantDetailView`) or pass a constant tuple (the popover, which has no steppers).
- **`Networking/FTSendPayload.swift`** (new): `nonisolated struct FTSendPayload: Sendable { host, port, width, height, pixelGridData, scaleFactor }` — the loop's endpoint+pixel payload, independent of whether the source is a `Variant` or a `FittedPreview`.
- **`Networking/FTSendPlan.swift`** (new): `nonisolated enum FTSendPlan` with `static func make(preview:host:port:layer:displayOffsetX:displayOffsetY:) -> (payload: FTSendPayload, offset: (x: Int, y: Int, z: Int))` — the pure, testable assembly of send parameters for a transient preview. **Offset convention** (per #0066's `FittedPreview.offsetX`/`offsetY` doc comment — "the centering placement the send adds to the display's stored offsetX/offsetY"): the final paint offset is `preview.offsetX + display.offsetX` and `preview.offsetY + display.offsetY` (each clamped non-negative independently before summing), and the layer is the display's stored `layer`, clamped into `FlaschenTaschenDisplay.layerRange`. For the common case of a display with no configured base offset (the default `0,0`), this reduces to exactly the centering offset, so the fitted image lands centered — satisfying the plan's "key requirement" without over-applying the addition for displays that do have a configured base offset.
- **`VariantDetailView.swift`**: refactored to delegate to `FTSendController` — `@State private var sendController = FTSendController()` replaces the old `isSending`/`sendTask`/`activeSend` `@State`; `isSending` becomes a computed property forwarding `sendController.isSending` so every read call site (banners, `.disabled(isSending)`, the Send/Stop label) is unchanged. `handleSendToDisplay` now builds an `FTSendPayload` from the variant's fields and calls `sendController.start(payload:frameOffset:onError:onStopped:)`, with `frameOffset` reading the live `sendLayer`/`sendOffsetX`/`sendOffsetY` `@State` each frame — preserving #0057's live-editable steppers exactly (no capture list on the closure, matching the pre-existing idiom where a `Task` closure captured `self` at creation time yet still read updated `@State` values, since `@State`'s storage is a shared box independent of the struct copy). `.onDisappear` now calls `sendController.stop()` directly.
- **`UI/GallerySendPopoverView.swift`** (new): shared popover content, own `NavigationStack` (registers `.navigationDestination(for: Variant.self)` so a saved-variant row can push `VariantDetailView` from inside the popover/sheet). Top to bottom: an FT display `Picker` (`@Query FlaschenTaschenDisplay`, default via `coordinator.resolveLastUsedDisplay(among:)` on first appearance via `.onChange(of: displays.map(\.id), initial: true)`, remembered via `coordinator.rememberLastUsedDisplay(_:)` on `.onChange(of: selectedDisplayID)`, `.disabled(isSending)`); the fitted preview loaded by `.task(id: selectedDisplayID)` calling `coordinator.fittedPreview(for:display:)` into `@State preview: FittedPreview?` (a `ProgressView` while nil), rendered via the new `PixelDataImageView` inside a `Color.black.aspectRatio(displayW/displayH, contentMode: .fit)` letterbox — per the plan, since the preview's own offset is always the centering offset, plain centering inside the display-aspect frame is visually exact with no manual offset math needed in the view; a Send/Stop button (disabled with no preview) driving `FTSendController` via `FTSendPlan.make(...)`; a Save-as-Variant button calling `coordinator.saveVariant(from:)`; and the `item.variants` list, each row a `NavigationLink(value: variant)`. When `displays.isEmpty`, shows `DisplayEmptyStateView` instead of a dead picker. `.onDisappear` calls `sendController.stop()`.
- **`GalleryListView.swift`**: each cell is now a `Button { selectedItem = item }` (was `NavigationLink(value: item)`) plus a per-cell derived `.popover(item:)` binding presenting `GallerySendPopoverView`; the `.navigationDestination(for: GalleryItem.self)` routing to `GalleryDetailView` is removed (the file itself is untouched — retirement is #0068). The root `NavigationStack` is unchanged. On iPhone the popover accepts the default sheet adaptation (`.presentationDragIndicator(.visible)` added; deliberately **not** forcing `.presentationCompactAdaptation(.popover)`, since the content is a full send surface that a cramped anchored bubble would clip). Deviation from the plan: the cell's `Button` + `.contextMenu` + `.popover` chain, inlined in the `ForEach`, made the Swift type checker time out ("unable to type-check this expression in reasonable time"); fixed by factoring it into a separate `@ViewBuilder private func galleryCellButton(for:)`, with no behavior change.
- **`VariantThumbnailView.swift`** / **`UI/PixelDataImageView.swift`** (new): the RGBA8888→`CGImage` render path (off-main `PixelGrid.fromRGBA8888` + `makeCGImage()`, `.interpolation(.none)`) moved into `PixelDataImageView(pixelGridData:width:height:)`; `VariantThumbnailView` is now a thin wrapper over it so the popover can render a transient preview's grid, which has no persisted `Variant` to key a `.task(id:)` off of. `PixelDataImageView` keys its `.task(id:)` on an internal `RenderKey { width, height, data }` (not just dimensions) so a re-render fires even if two different display selections happen to share fit dimensions but different pixel content.
- **`DisplayRegistryView.swift`** / **`UI/DisplayEmptyStateView.swift`** (new): the private `emptyState` body (icon, Scan/Add buttons, the manual-entry sheet and scan-result alert) moved verbatim into `DisplayEmptyStateView(coordinator:)`; `DisplayRegistryView.emptyState` is now a one-line wrapper. `DisplayRegistryView` keeps its own separate `isScanning`/`showManualEntry`/`scanResultMessage` state and `scan()` for its toolbar's Scan/Add menu (unaffected — that path exists regardless of whether the list is empty).
- No changes to `GalleryDetailView.swift` (left in place, unreachable from the grid; retirement is #0068).

## Verification

- `cd PixelArtGalleryKit && swift test` — full suite passed: **179 tests in 24 suites** (up from 173 tests in 23 suites before this change), confirming the new suite actually executed rather than a "0 tests run" false-green. New `FTSendPlanTests` suite, all 6 tests observed passing by name: `payloadCarriesThePreviewsFitDimensionsAndGridVerbatim`, `offsetIsThePreviewsCenteringOffsetWhenTheDisplayHasNoStoredOffset`, `offsetAddsTheDisplaysStoredOffsetToTheCenteringOffset`, `negativeStoredOffsetIsClampedToZeroBeforeSumming`, `layerIsCarriedThroughUnchangedWhenAlreadyInRange`, `outOfRangeLayerIsClampedIntoOneThroughFifteen`. All pre-existing suites (including `GalleryCoordinatorTests`, `SendDefaultsSeedTests`, `FTPaintTargetTests`) passed unmodified, confirming the `FTSendController` extraction didn't regress `VariantDetailView`'s existing send behavior at the unit level.
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=macOS' build` — **BUILD SUCCEEDED**.
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` — **BUILD SUCCEEDED**.
- **Not exercised here (pending a live app run, per this issue's mandatory-run requirement and the #0064 lesson that green builds/tests can still be wrong on screen):** tapping a gallery cell actually opens the popover (macOS anchored popover / iPhone sheet) with no push to `GalleryDetailView`; the dropdown visibly defaults to the last-used display and remembers a change across reopen; a non-matching-aspect display shows a visibly un-stretched, letterboxed/centered fitted preview; Send visibly toggles to Stop and dismissing mid-send visibly stops/clears the display; Save visibly adds exactly one row to the saved-variants list without duplicating on repeat; a saved-variant row visibly pushes `VariantDetailView` inside the popover. These are UI/runtime behaviors builds and unit tests cannot exercise and were not run in this environment.

## Files changed

- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Networking/FTSendPayload.swift` — new: `FTSendPayload`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Networking/FTSendPlan.swift` — new: `FTSendPlan.make(...)`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/ViewModels/FTSendController.swift` — new: the extracted continuous-send loop.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/GallerySendPopoverView.swift` — new: the popover content view.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/PixelDataImageView.swift` — new: raw-grid renderer extracted from `VariantThumbnailView`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/DisplayEmptyStateView.swift` — new: Scan/Add empty state extracted from `DisplayRegistryView`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/GalleryListView.swift` — cell `Button` + per-cell `.popover`, dropped the `GalleryItem` `navigationDestination`, factored the cell into `galleryCellButton(for:)`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift` — delegates the send loop to `FTSendController`; behavior (live layer/x/y steppers, banners, stop-clear on disappear) unchanged.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantThumbnailView.swift` — now a thin wrapper over `PixelDataImageView`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/DisplayRegistryView.swift` — `emptyState` now wraps `DisplayEmptyStateView`.
- `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/Networking/FTSendPlanTests.swift` — new: 6 tests for `FTSendPlan.make(...)`.
- No changes to `GalleryDetailView.swift`, `DisplaySendPickerView.swift`, or `VariantCreationView.swift` (retirement is #0068).

## Gotchas

- The plan's `FTSendPlan.make(preview:host:port:layer:)` signature didn't include the display's stored offset as a parameter, while the plan's separate "CRITICAL send-offset detail" callout said the offset must additionally include the display's stored offset. Resolved by extending the signature to take `displayOffsetX`/`displayOffsetY` explicitly (see `## Fix` above) — this matches #0066's own `FittedPreview` field-justification comment, which is the more authoritative source since it was written when the field's contract was defined.
- `VariantDetailView`'s `onStopped`/`onError` callbacks receive a generic message from `FTSendController` (it has no `displayName` to format with); the view's closures ignore that string and build their own `"Stopped sending to \(displayName)"` message from state captured at `handleSendToDisplay` call time, exactly reproducing the pre-extraction text. The popover does the same.
- `GalleryListView`'s cell `Button` + `.contextMenu` + `.popover` chain, when written inline inside the `ForEach`, caused an "unable to type-check this expression in reasonable time" compiler error; fixed by factoring it into `galleryCellButton(for:)` (see `## Fix`).

## Notes

Relevant code:
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/GalleryListView.swift` — the grid; each cell currently `NavigationLink(value: item)`. Change to present a popover anchored on the tapped cell (track a `selectedItem` and use `.popover(item:)`).
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/GalleryDetailView.swift` — the pushed detail being replaced (retired in [#0068](0068.md), but this issue stops routing to it).
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/ViewModels/GalleryCoordinator.swift` — `fittedPreview(for:display:)`, `saveVariant(from:)`, and last-used-display default from [#0066](0066.md).
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/PixelGridView.swift` / `PixelGridViewModel.swift` — aspect-correct grid rendering to show the fitted preview (already renders true aspect via `fitScale`). May need to render from a transient `FittedPreview` (grid data) rather than only a persisted `Variant`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Networking/FTDisplayClient.swift` + `FTPaintTarget.swift` — the send path (`send(width:height:pixelGridData:scaleFactor:to:port:offset:)`); the continuous-send loop pattern lives in `VariantDetailView.swift` (#0057). Send from the transient preview at the centering offset (x/y from `FittedPreview`, z = display's stored `layer`).
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Models/FlaschenTaschenDisplay.swift` — `@Query` source for the dropdown; `displayName`, `resolution`, `endpoint`.

Design points (planner to finalize):
- **Popover presentation**: `.popover(item: $selectedItem)` on the grid. On iOS compact width a `.popover` adapts to a sheet by default — decide whether to force `.presentationCompactAdaptation(.popover)` or accept the sheet adaptation on iPhone (recommend accepting the adaptation so it's usable on a phone; a true tiny popover is bad on iPhone). Document the choice. Give the popover a sensible size on macOS (`.frame`/`.presentationDetents` as appropriate).
- **Dropdown**: a `Picker`/`Menu` over `@Query FlaschenTaschenDisplay`, default selection = last-used (from #0066), updating last-used on change. Empty state: if no displays, offer Scan/Add (reuse `DisplayRegistryView`'s affordances) instead of a dead popover.
- **Fitted preview**: on display selection, call `coordinator.fittedPreview(for:display:)` (async; show a brief spinner) and render the fitted grid via `PixelGridView`-style rendering at true aspect. Optionally show it within the display-canvas frame (letterboxed) to convey the centered placement — nice-to-have, planner's call.
- **Send**: a Send (and Stop) button that runs the continuous-send loop for the fitted grid at offset `(x: preview.offsetX, y: preview.offsetY, z: display.layer)`. Reuse/extract the loop from `VariantDetailView` so it can drive a transient preview (not only a persisted `Variant`) — factor the send-loop core into something callable from both. Keep the #0052/#0053 stop-clear + `.onDisappear` teardown semantics.
- **Saved variants list**: show `item.variants`; each row opens the existing editor/detail (`VariantDetailView`) or a save action promotes the current transient preview via `coordinator.saveVariant(from:)`. A **Save** affordance persists the current fitted preview into the list; the transient default must not silently accumulate saved variants.
- **Platform**: one shared view with `#if os` for popover sizing / presentation only.

Testing: extract any new pure logic (default-display resolution wiring, send-parameter assembly from a `FittedPreview`) into testable helpers and cover with Swift Testing. UI wiring itself isn't unit-testable — the required bar is both-platform builds AND **actually running the app** to confirm: popover opens on tap, dropdown defaults to last-used, the fitted preview is visibly un-stretched for a non-matching aspect, Send is wired, and Save adds to the variants list. The prior UI (#0064) passed builds/tests but was wrong in the running app — running it is mandatory here, not optional.

## Relation

- Parent: [#0065](0065.md).
- Depends on: [#0066](0066.md) (transient preview, save path, last-used default) and [#0062](0062.md) (fit/centering).
- Retirement of the replaced screens happens in: [#0068](0068.md).

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-07-11 | plan | claude-fable-5 | 7,393 | 522 | 419,899 | 89,461 | $1.64 |
| 2026-07-11 | implement | claude-sonnet-5 | 2,474 | 8,291 | 7,515,495 | 199,247 | $2.09 |

**Total: $3.73**
