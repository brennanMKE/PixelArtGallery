# 0018 — Let the user name an imported image instead of hardcoding "Imported Image"

| | |
|---|---|
| **Status** | resolved |
| **Module** | UI / ViewModels |
| **Platform** | All |
| **First seen** | 2026-06-25 |
| **Closed** | 2026-06-29 |
| **Commit** | 8811cb1 |

## Description

Every imported image is labeled "Imported Image", so the gallery fills with identically-named items and they can't be told apart. The user needs a way to set (and later edit) the name.

## Expected behavior

When importing, the user can enter a name for the image — ideally prefilled with a sensible default (e.g. the source filename where the picker provides one, or "Imported Image" as a fallback). The chosen name is stored on the `GalleryItem` and shown in the list and detail views. Renaming an existing gallery item afterward would also be good.

## Actual behavior

The name is hardcoded. `GalleryListView.swift:144` does `let imageName = "Imported Image"` and passes it to `coordinator.createGalleryItem(name:imageData:)`, so every item gets the same label.

## Notes

- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/GalleryListView.swift` (~line 144) — the hardcoded name at the import call site.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/ImagePickerView.swift` — the picker passes only `Data` via `onImageSelected: (Data) -> Void`; it doesn't surface a filename. macOS `NSOpenPanel` has the source `URL` (could derive a default name from `url.deletingPathExtension().lastPathComponent`); iOS `PHPickerResult.itemProvider.suggestedName` can supply one. Widening the closure to pass an optional suggested name would enable a good default.
- `GalleryItem.originalName` already stores the name and is rendered in `GalleryListView`/`GalleryDetailView`; the duplicate-prevention check (#0014) keys on content hash, not name, so naming is independent of de-dup.
- Two reasonable pieces: (1) a naming prompt/sheet at import time prefilled with the default; (2) inline rename of an existing item (e.g. a context-menu "Rename" → coordinator method that updates `originalName` and saves).
- PRD §1 lists "filename" as stored gallery metadata, so capturing the real source name is in scope.

## Root cause

The import call site in `GalleryListView` hardcoded `let imageName = "Imported Image"`, and `ImagePickerView.onImageSelected` was typed `(Data) -> Void`, so the source filename (macOS) and `PHPickerResult.itemProvider.suggestedName` (iOS) were discarded before they could become a default. There was also no path to edit a name after import — the coordinator only had a `renameDisplay` for FT displays, nothing for gallery items.

## Fix

Widened `ImagePickerView.onImageSelected` to `(Data, String?) -> Void`, deriving the suggestion from `url.deletingPathExtension().lastPathComponent` on macOS and `itemProvider.suggestedName` on iOS. Picking now stages a `PendingImport`, which drives a new `ImportNamingView` sheet (styled after `ManualDisplayEntryView`) prefilled with the suggested name — falling back to `"Imported Image"` — so the user confirms/edits before `createGalleryItem` runs. The suggestion→effective-name defaulting (strip extension, trim, fall back) is factored into a pure `nonisolated func effectiveImportedImageName(from:)`. Added `GalleryCoordinator.renameGalleryItem(_:to:)` mirroring `renameDisplay` (trim, ignore empty, update `originalName`, save through the `ModelContext`), surfaced via a context-menu "Rename" on the list row and a toolbar "Rename" on `GalleryDetailView`, each backed by a small text-field alert. Duplicate detection, storage, and the image-display path were left untouched.

## Verification

- `cd PixelArtGalleryKit && swift test` — 69 tests passed, 0 failures, including the three new ones: `testRenameGalleryItemUpdatesAndPersistsName`, `testRenameGalleryItemIgnoresEmptyOrWhitespaceName`, and `testEffectiveImportedImageNameDefaulting` (all confirmed present and passing in the output).
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO build` — **BUILD SUCCEEDED**.
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` — **BUILD SUCCEEDED**.

## Files changed

- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/ImagePickerView.swift` — widened `onImageSelected` to pass an optional suggested name (macOS URL base name, iOS `suggestedName`); added `defaultImportedImageName` constant and the pure `effectiveImportedImageName(from:)` helper.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/ImportNamingView.swift` — new naming sheet prefilled with the suggested name, calling back with the chosen name on confirm.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/GalleryListView.swift` — stage picked image as a `PendingImport`, present the naming sheet, and add a context-menu "Rename" with a text-field alert.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/GalleryDetailView.swift` — added a toolbar "Rename" button and rename alert.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/ViewModels/GalleryCoordinator.swift` — added `renameGalleryItem(_:to:)`.
- `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/ViewModels/GalleryCoordinatorTests.swift` — added rename + defaulting-helper tests.
