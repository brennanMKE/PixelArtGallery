# 0057 — Send UI is buried at the bottom of the variant view and cannot be adjusted while sending

| | |
|---|---|
| **Status** | resolved |
| **Module** | UI |
| **Platform** | All |
| **First seen** | 2026-07-09 |
| **Commit** | 5bc9317 |
| **Closed** | 2026-07-09 |

## Description

The Send UI is hidden down at the bottom of the variant view and seems difficult to get to. Sending is the primary reason to move to a variant, so it should be presented near the top. From there it should be quick to tap the Send button without messing with the layer and offset values. And while a send is active it should be possible to adjust the layer, x and y values so the next frame sends the changed values — the user should not have to stop a send to change them.

## Plan

**Depends on #0056** — the x/y offset fields (and per-display stored defaults for layer/x/y) come from that issue, so implement this one *after* #0056 lands; the "live x/y" parts of this plan apply to whatever controls #0056 adds.

### Current state and why parameters are frozen

`PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift` lays out its `ScrollView` as `infoCard → previewSection (340pt tall) → exportSection → sendSection → status banners`, so the send controls sit below the fold on both platforms (the same view serves macOS and iOS). Parameters are frozen during a send for two reasons:

1. `handleSendToDisplay()` reads `sendLayer` **once** at start, clamps it into a local `let layer`, and captures it (with host/port/width/height/scaleFactor) into the `ActiveSend` struct (#0053) and into the send loop's closure — the `while !Task.isCancelled` loop always sends `offset: (x: 0, y: 0, z: layer)` from that one-time capture.
2. The layer `Stepper` is explicitly `.disabled(isSending || selectedDisplay == nil)`, a leftover from #0047/#0050 when mid-send changes had no effect anyway.

`stopSending()` (#0053) clears by sending 3 all-black frames to the *captured* `ActiveSend` layer. That is correct today precisely because the loop can never change layer; once layer/x/y become live, a mid-send change would strand the last frame on the old layer (or old offset region) forever — the FT server only drops it on its ~15s layer timeout, and black-is-transparent clearing only reaches the layer we last targeted.

### Files and functions

- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift` — `body` (section order), `sendSection` (control layout + `disabled` modifiers), `handleSendToDisplay()` (loop reads), `ActiveSend` (must track last-*painted* layer/x/y, not start-of-send values), `stopSending()` (clear target), plus a new shared clear-frame helper.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Networking/FTDisplayClient.swift` — no changes expected; `send(...offset:)` already takes x/y/z per call (x/y wiring itself is #0056).
- `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/Networking/FTDisplayClientTests.swift` — existing all-black clear-frame packet test from #0053 keeps covering the clear payload.

### Approach

1. **Move the send section to the top.** Reorder `body`'s `VStack` to `infoCard → sendSection → (send/status banners) → previewSection → exportSection` so the send controls and their banners are visible without scrolling on both macOS and iOS (one shared view — no platform forks needed).
2. **One-tap send with defaults.** Within `sendSection`, put the display picker and the prominent **Send Now / Stop Sending** button first; layer (and, post-#0056, x/y) sit below as optional adjustments. The existing `seedSendLayer()` / `onChange(of: selectedDisplayID)` seeding (extended by #0056 to seed x/y from the display's stored defaults) already means a bare tap sends with the display's defaults — verify nothing forces the user through the steppers/fields first.
3. **Live per-frame reads of layer/x/y.** Remove `isSending` from the controls' `.disabled(...)` (keep `selectedDisplay == nil`). In the send loop — a `Task {}` created in the view, so it runs on the main actor and may read `@State` safely — read and clamp the *current* `sendLayer` (and x/y) at the top of each iteration and pass them to `client.send(offset:)`, instead of using the start-of-send capture. Endpoint (host/port) and geometry stay captured at start; only layer/x/y go live. Display picker stays disabled (or its changes ignored) during a send, matching #0053's rationale.
4. **Clear a layer/region the user switches away from.** Factor #0053's clear (3× all-black frame, best-effort, detached-capable) into a helper, e.g. `sendClearFrame(host:port:width:height:scaleFactor:x:y:layer:)`. In the loop, when this frame's layer or x/y differs from the last-painted values, first send a clear frame to the **old** layer at the **old** x/y (same width/height, so the old region is erased), then paint the new values. After each successful frame, update `activeSend` so it always holds the last-*painted* layer/x/y — `stopSending()` then keeps working unchanged, clearing exactly what is on the display, and the #0052 `.onDisappear` path inherits the same correctness. Black remains transparent only on layers 1–15; `clampedLayer` already guarantees we never paint or clear layer 0.
5. **Keep the stop-clear semantics from #0053 intact**: clear only on user-initiated stop (the loop's `defer` still nils `activeSend` on error exit), clears stay detached so they survive view teardown.

### Verification

- `cd PixelArtGalleryKit && swift test` — all existing tests pass, including #0053's `allBlackFrameProducesBlackPixelsOnChosenLayer`; if the clear helper or per-frame parameter selection is factored into testable (non-view) code, add a test that a layer/offset change produces a clear to the old target before the paint to the new one.
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=macOS' build` and `-destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` — both must succeed (the layout move touches the shared view used by both platforms).
- Manual on the FT network: open a variant — send controls visible near the top; tap Send with no other input — frame appears using the display's defaults; while sending, step the layer (and change x/y) — the image moves on the next frame and the old layer/region clears; tap Stop or navigate away — the display clears.

> 🟢 Resolved 2026-07-09 — Send controls now sit right after the info card and the send loop reads/clamps layer/x/y live each frame, clearing the old target before painting a changed one so stop/onDisappear always erase what's on the display.

## Root cause

`VariantDetailView`'s `body` laid out `infoCard → previewSection (340pt) → exportSection → sendSection`, pushing the send controls (and their status banners) below the fold on both platforms even though sending is the primary reason to open a variant. Separately, the layer/x/y `Stepper`s carried `.disabled(isSending || selectedDisplay == nil)` — a leftover from before #0056 added the offset fields, when mid-send changes had no effect anyway — and `handleSendToDisplay()`'s continuous-send loop closed over `layer`/`offsetX`/`offsetY` captured once at the top of the function, so the `while !Task.isCancelled` loop always resent the same start-of-send values regardless of later stepper edits. Because the loop could never change layer/offset, `ActiveSend` only ever needed to record the start-of-send values for `stopSending()`'s clear frame; once layer/x/y become live per-frame, a mid-send switch would otherwise strand the previously painted layer/region (the FT server only drops it after its ~15s layer timeout).

## Fix

- Reordered `body` to `infoCard → sendSection → status banners → previewSection → exportSection` (one shared view, no platform forks needed) so the send controls are visible without scrolling on both macOS and iOS.
- Removed `isSending` from the layer/x/y `Stepper`s' `.disabled(...)` (kept `selectedDisplay == nil`). Added `.disabled(isSending)` to the display **Picker** — it had no disabled modifier at all previously, which was an existing gap now worth closing since the endpoint is still captured once at send start and must not desync from the picker mid-send.
- `handleSendToDisplay()`'s send loop now reads and clamps the *current* `sendLayer`/`sendOffsetX`/`sendOffsetY` at the top of every iteration (endpoint host/port and the variant's pixel/geometry data stay captured once at start, per the plan). A local `painted: FTPaintTarget?` tracks what the loop last actually painted; when the next frame's layer/offset differs from `painted`, the loop first awaits a clear of the *old* target, then paints the new frame, then updates both `painted` and the `@State activeSend` to the newly painted values.
- Replaced the private `ActiveSend` struct with a new `nonisolated public struct FTPaintTarget` (in `Networking/FTPaintTarget.swift`) carrying the endpoint/geometry/layer/offset, plus two pure helpers: `requiresClear(beforePaintingLayer:offsetX:offsetY:)` and `painted(layer:offsetX:offsetY:)`. Marked `nonisolated` to opt out of the package's `.defaultIsolation(MainActor.self)` build setting, matching the existing convention for other cross-actor Sendable value types (`PixelGrid`, `DiscoveredFTDisplay`, etc.) — without it, the type is implicitly MainActor-isolated and can't be read from a nonisolated test function or a detached task.
- Factored the 3x best-effort all-black clear-frame retry (previously inlined in `stopSending()`) into `FTDisplayClient.sendClearFrame(to:)`, an actor method shared by both the mid-send clear (awaited in the loop, so ordering is guaranteed) and `stopSending()`'s detached final clear.
- `stopSending()`, the #0053 stop-clear and #0052 `.onDisappear` path, is otherwise unchanged in behavior: it still captures `activeSend` before cancelling and clears exactly that target — which now reflects the *last painted* layer/offset rather than the start-of-send values, so it keeps clearing precisely what's on the display after any number of mid-send switches.

No plan divergences beyond the two call-outs above (the picker's missing `.disabled`, and the `nonisolated` fix required for the new pure type to compile under this package's default MainActor isolation).

## Verification

- `cd PixelArtGalleryKit && swift test` — all 133 tests across 19 suites pass, including the new `FTPaintTargetTests` suite (6 tests: `noClearWhenLayerAndOffsetUnchanged`, `clearRequiredWhenLayerChanges`, `clearRequiredWhenOffsetXChanges`, `clearRequiredWhenOffsetYChanges`, `clearRequiredWhenBothLayerAndOffsetChange`, `paintedUpdatesLayerAndOffsetOnly`, `sequenceOfFramesClearsOnlyOnChange`) and the existing `FTDisplayClientTests.allBlackFrameProducesBlackPixelsOnChosenLayer`. `sequenceOfFramesClearsOnlyOnChange` walks a simulated frame sequence `(5,0,0)→(5,0,0)→(7,0,0)→(7,2,3)→(7,2,3)` and asserts exactly the two changed frames trigger a clear of the *old* target, in order, before the new target is considered painted — directly exercising the clear-then-paint decision the send loop makes each frame.
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=macOS' build` — **BUILD SUCCEEDED**.
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` — **BUILD SUCCEEDED**.
- Manual verification on a live FT network was not performed by this pass (no display was available in this environment); the reviewer or user should confirm the "Manual on the FT network" checks from the Plan (send controls visible near the top with no scrolling; tap Send with defaults; live layer/x/y edits move the image and clear the old layer/region on the next frame; Stop/navigate-away clears the display).

## Files changed

- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift` — reordered `body`; removed `isSending` from layer/x/y `disabled`; added `.disabled(isSending)` to the display picker; replaced the frozen-at-start send loop with a live per-frame read/clamp + clear-on-switch loop; replaced the private `ActiveSend` struct with `FTPaintTarget`; simplified `stopSending()` to call `FTDisplayClient.sendClearFrame(to:)`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Networking/FTPaintTarget.swift` (new) — `nonisolated public struct FTPaintTarget` with `requiresClear(beforePaintingLayer:offsetX:offsetY:)` and `painted(layer:offsetX:offsetY:)`.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Networking/FTDisplayClient.swift` — added `sendClearFrame(to:)`, factoring the 3x best-effort all-black retry previously inlined in the view.
- `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/Networking/FTPaintTargetTests.swift` (new) — unit tests for the clear-decision logic.

## Gotchas

- The package target sets `.defaultIsolation(MainActor.self)` in `Package.swift`, so any new Sendable value type meant to cross actor boundaries (used from a detached task, an actor method, or a plain test function) must be explicitly marked `nonisolated` or it becomes implicitly MainActor-isolated and fails to compile outside MainActor context — this bit the first pass of `FTPaintTarget` and its test file.
- The mid-send clear is `await`ed in-line in the loop (not detached), so a layer/offset switch adds ~360ms (3 retries × 120ms) of latency before the new frame paints. This is intentional per the plan (guarantees clear-before-paint ordering) and only affects the frame immediately after a user-initiated switch, not steady-state refreshes.

## Steps to reproduce

1. Open a variant.
2. Scroll to find the send controls.
3. Start a continuous send, then try to change the layer (or offset) without stopping.

## Expected behavior

The send controls sit near the top of the variant view; tapping Send works immediately with the display's defaults; layer, x and y remain editable during an active send and take effect on the next frame.

## Actual behavior

The send controls are at the bottom of the view, and send parameters cannot be changed while a send is running.

## Notes

Likely code: `VariantDetailView.swift` — layout of the send section, and the continuous-send loop (see #0050/#0053), which captures its parameters into `ActiveSend` at start; live adjustment means the loop must read the current layer/x/y each frame instead (while the stop-clear from #0053 must still erase the layer(s) actually painted, including a layer switched away from mid-send). Depends on #0056 for the x/y offset fields existing at all.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-07-09 | plan | claude-fable-5 | 4,074 | 5,018 | 101,939 | 29,970 | $0.77 |
| 2026-07-09 | implement | claude-sonnet-5 | 70 | 21,350 | 2,546,175 | 111,421 | $1.00 |
| 2026-07-09 | review | claude-opus-4-8 | 4,086 | 3,561 | 342,243 | 47,299 | $0.58 |

**Total: $2.35**
