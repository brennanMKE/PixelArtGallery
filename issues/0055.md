# 0055 — Variant view does not auto-select the display matching the variant's dimensions

| | |
|---|---|
| **Status** | resolved |
| **Module** | UI |
| **Platform** | All |
| **First seen** | 2026-07-09 |
| **Closed** | 2026-07-09 |
| **Commit** | c9d8664 |

## Description

When selecting a variant such as 8x8 while a display which is 8x8 exists, the app should automatically select that display in the send UI. Today the user has to change the display selection manually, often, which is cumbersome.

## Plan

### How selection works today, and why it ignores geometry

`VariantDetailView.sendSection` (`PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift`, the `.onChange(of: displays.map(\.id), initial: true)` block around lines 228–243) seeds and maintains `selectedDisplayID` by calling `FlaschenTaschenDisplay.preferredSelection(current:among:)` (`PixelArtGalleryKit/Sources/PixelArtGalleryKit/Models/FlaschenTaschenDisplay.swift`, added for #0032). That pure function only sees `(id, source)` pairs: it keeps a still-valid `current`, otherwise prefers the seeded default display (`source == "default"`), otherwise the first candidate. Display geometry (`displayWidth`/`displayHeight`) and the variant's `targetWidth`/`targetHeight` are never consulted, so on open the picker lands on the default/first display regardless of dimensions. There is no other persisted "last used display" store — the `@State selectedDisplayID` starts `nil` on every view open, so the seeding call is the single place to fix.

### Approach

1. **Extend the pure selection rule with geometry** in `FlaschenTaschenDisplay.swift`: change `preferredSelection` to also take the variant's dimensions and geometry per candidate, e.g. `preferredSelection(current: UUID?, variantWidth: Int, variantHeight: Int, among candidates: [(id: UUID, source: String, width: Int, height: Int)]) -> UUID?`. Keep it a static pure function on plain tuples (no SwiftData) so it stays unit-testable. New rule order:
   - **Match set:** candidates whose `width × height` equals the variant's `targetWidth × targetHeight` (exact equality, no rotation/aspect matching).
   - **Current already matches:** if `current` is in the match set, keep it — never stomp a selection that already fits.
   - **Tie-break among several matches:** otherwise pick the *first* match in candidate order (candidates arrive from the `@Query` sorted by `displayName`, so this is deterministic: the alphabetically first matching display). This follows the issue's Notes; no default-source preference inside the match set.
   - **Fallback when none match:** exactly today's behavior — keep a still-valid `current`, else the default-source display, else the first candidate, else `nil`. This preserves the #0032 guarantees for registries with no geometry match.
2. **Update the call site** in `VariantDetailView.sendSection`: pass `variant.targetWidth` / `variant.targetHeight` and map candidates to `(id:source:width:height:)` from each display's `displayWidth`/`displayHeight`. Also broaden the `.onChange` trigger key from `displays.map(\.id)` to a value that includes each display's geometry (e.g. `displays.map { "\($0.id)|\($0.displayWidth)x\($0.displayHeight)" }`) and add an `.onChange` on the variant's dimensions, so editing a display's geometry in Settings or resizing the variant via Edit Dimensions re-evaluates the selection. `seedSendLayer()` already reseeds via the existing `.onChange(of: selectedDisplayID)`, so the layer follows automatically.
3. **Accepted trade-off (note in `## Fix` if kept):** because a valid-but-non-matching `current` is replaced by a geometry match, a user's explicit non-matching pick can be overridden if the registry or variant dimensions change while the view is open. That is rare and is exactly the auto-select behavior this issue asks for; on plain view open `current` is always `nil`, so the common path is a clean auto-select.
4. **Update tests** in `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/UI/DisplayPreferredSelectionTests.swift` (Swift Testing): adapt the five existing #0032 cases to the new signature (they exercise the none-match fallback), and add geometry cases — auto-selects the sole matching display over the default; keeps a current selection that already matches when several match; picks the first (name-sorted) match when current doesn't match; replaces a non-matching current with a match; falls back to default/first/nil when nothing matches; empty candidate list returns `nil`.

### Verification

- `cd PixelArtGalleryKit && swift test` — confirm the updated `DisplayPreferredSelectionTests` suite actually runs and passes (read the output, not just the exit code).
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=macOS' build`
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build`
- Manual sanity check if convenient: register an 8×8 and a 64×64 display, open an 8×8 variant, and confirm the picker opens on the 8×8 display.

> 🟢 Resolved 2026-07-09 — the variant send picker now auto-selects the display whose geometry exactly matches the variant's dimensions, keeping an already-matching selection and falling back to the #0032 default/first/nil rule when none match.

## Root cause

`VariantDetailView.sendSection`'s `.onChange(of: displays.map(\.id), initial: true)` seeded `selectedDisplayID` by calling `FlaschenTaschenDisplay.preferredSelection(current:among:)` (#0032), which only ever saw `(id, source)` pairs. It kept a still-valid `current`, otherwise preferred the seeded default display, otherwise the first candidate — display geometry (`displayWidth`/`displayHeight`) and the variant's `targetWidth`/`targetHeight` were never consulted. On every view open `selectedDisplayID` starts `nil`, so the picker always landed on the default/first display regardless of dimensions, forcing a manual switch.

## Fix

Followed the plan as written, with one small structural addition not called out in the plan text: rather than duplicating the `preferredSelection` call across three `.onChange` closures, extracted a private `updateSelectedDisplay()` helper on `VariantDetailView` that all three call, to avoid repeating the mapping/argument boilerplate. No behavioral divergence from the plan.

1. `FlaschenTaschenDisplay.preferredSelection` (`PixelArtGalleryKit/Sources/PixelArtGalleryKit/Models/FlaschenTaschenDisplay.swift`) now takes `variantWidth`/`variantHeight` and candidates of `(id, source, width, height)`. It first computes the match set (`width == variantWidth && height == variantHeight`); if non-empty it keeps `current` when `current` is itself a match, else returns the first match in candidate order. If the match set is empty it falls through to the original #0032 rule unchanged (still-valid `current`, else default-source, else first, else `nil`).
2. `VariantDetailView.sendSection` (`PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift`) now:
   - Widens the registry `.onChange` key from `displays.map(\.id)` to `displays.map { "\($0.id)|\($0.displayWidth)x\($0.displayHeight)" }` so editing a display's geometry in Settings re-triggers selection.
   - Adds `.onChange(of: variant.targetWidth)` and `.onChange(of: variant.targetHeight)` so resizing the variant (Edit Dimensions) re-evaluates the selection too.
   - Routes all three through a new `updateSelectedDisplay()` helper that calls `preferredSelection` with `variant.targetWidth`/`targetHeight` and the displays mapped to `(id, source, width, height)`.
   - `seedSendLayer()` continues to run off the existing `.onChange(of: selectedDisplayID)`, so the send layer follows the new selection automatically — no change needed there.

## Verification

- `cd PixelArtGalleryKit && swift test` — full suite ran: "Test run with 118 tests in 17 suites passed after 0.160 seconds." Confirmed by reading the output that the `DisplayPreferredSelectionTests` suite executed all 12 tests (the pre-existing `makeDisplayModelFallsBackToDefaultsWhenDimensionsMissing` case, the original 5 #0032 cases adapted to the new signature, plus `autoSelectsSoleMatchingDisplayOverDefault`, `keepsCurrentSelectionThatAlreadyMatchesAmongSeveralMatches`, `picksFirstMatchInCandidateOrderWhenCurrentDoesNotMatch`, `replacesNonMatchingCurrentWithAMatch`, `fallsBackToDefaultFirstOrNilWhenNothingMatches`, `returnsNilForEmptyCandidateListEvenWithCurrent`) — line `✔ Suite DisplayPreferredSelectionTests passed after 0.052 seconds.` plus each individual `✔ Test ...` line for that suite appeared in the output.
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=macOS' build` — `** BUILD SUCCEEDED **`.
- `xcodebuild -project PixelArtGallery.xcodeproj -scheme PixelArtGallery -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` — `** BUILD SUCCEEDED **`.
- Manual sanity check (registering displays and opening a variant in the running app) was not performed in this pass; the unit tests directly exercise the new selection rule end to end at the pure-function level, and both platform builds compile the updated `VariantDetailView` call site cleanly.

## Files changed

- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Models/FlaschenTaschenDisplay.swift` — extended `preferredSelection` with geometry matching.
- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/UI/VariantDetailView.swift` — pass variant/display geometry into `preferredSelection`, broaden the `.onChange` triggers, add `updateSelectedDisplay()` helper.
- `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/UI/DisplayPreferredSelectionTests.swift` — adapted the five #0032 cases to the new signature and added six geometry-matching cases.

## Steps to reproduce

1. Register two displays with different geometries (e.g. 8x8 and 64x64).
2. Open an 8x8 variant.
3. Look at the display picker in the send section.

## Expected behavior

The display whose geometry matches the variant's dimensions (8x8) is selected automatically when the variant view opens.

## Actual behavior

The previously selected (or default) display stays selected regardless of the variant's dimensions, and the user must switch it manually every time.

## Notes

Likely code: `VariantDetailView.swift` (display picker / send section) and `FlaschenTaschenDisplay` (width/height). If several displays match the variant's dimensions, keep the current selection when it already matches, otherwise pick the first match; fall back to existing behavior when nothing matches.

## Work log

| Date | Phase | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|---|
| 2026-07-09 | plan | claude-fable-5 | 4,080 | 7,755 | 179,679 | 30,169 | $0.99 |
| 2026-07-09 | implement | claude-sonnet-5 | 44 | 11,725 | 922,442 | 48,599 | $0.42 |

**Total: $1.41**
