# 0051 — FT layer is never applied: the footer's leading 0x00 byte makes the server ignore the offset, so images always land on layer 0

| | |
|---|---|
| **Status** | resolved |
| **Module** | Networking |
| **Platform** | All |
| **First seen** | 2026-07-07 |
| **Closed** | 2026-07-07 |
| **Branch** | issue/0051 |

## Description

Images sent to a Flaschen Taschen display always paint on layer 0 (the background), regardless of the layer chosen in Settings or the Send stepper (#0047). The app encodes the offset/layer as a trailing footer that begins with a `0x00` byte, and the reference FT server's parser bails on that byte before it ever reads the layer. On a display running demos on higher layers, the app's image is composited *behind* them and appears to do nothing.

## Steps to reproduce

1. Send any variant to an FT display that is also showing demo content on a non-background layer.
2. The app reports success, but nothing visibly changes on the panel.

## Expected behavior

The chosen layer (Settings default 5, or the Send stepper value, 1–15) is actually applied, so the image paints on that layer and appears in front of lower layers.

## Actual behavior

The image always lands on layer 0. The layer value never reaches the server.

## Long Description

`FTDisplayClient.makePacket` builds the datagram as `PPM` + `footerStart (0x00)` + `"<x>\n<y>\n<z>\n"` (`Networking/FTDisplayClient.swift`, ~lines 172, 202–204).

The reference server parses trailing offsets in `server/ppm-reader.cc`:

```c
info->offset_x = readNextNumber(&start, end, NULL);
if (start != NULL) info->offset_y = readNextNumber(&start, end, NULL);
if (start != NULL) info->layer    = readNextNumber(&start, end, NULL);
```

and `readNextNumber` does `skipWhitespace()` (which uses `isspace()`) then `strtol()`. `0x00` is **not** `isspace`, so it is not skipped; `strtol` on a leading `0x00` converts nothing, so `readNextNumber` returns 0 **and sets `start = NULL`**. With `start == NULL`, `offset_y` and `layer` are never read — the layer defaults to 0 (background).

Verified against upstream `hzeller/flaschen-taschen`:
- `server/ppm-reader.cc` — `parseOffsets`/`readNextNumber` as above; no NUL separator is expected.
- `api/lib/udp-flaschen-taschen.cc` — the reference client encodes offset/layer as a PPM **header comment** `#FT: <x> <y> <z>` (written into the header as `"P6\n%d %d\n#FT: %d %d %d\n255\n"`), not a trailing footer, and never emits a `0x00`.
- `doc/protocols.md` — trailing offsets are "decimal numbers, separated by space", x then y then z; z=0 is background, higher z overlays; unrefreshed layers fade out over time (which is why #0050's periodic resend is needed).

## Fix options

1. **Minimal:** drop the `0x00` prefix so the footer is `"<x>\n<y>\n<z>\n"`. This parses correctly (x, y, layer) since the server delimits the footer by byte count, not a marker byte.
2. **Canonical (preferred):** stop appending a trailing footer and instead emit the offset as a PPM header comment `#FT: <x> <y> <z>` between the `W H` line and the `255` maxval — matching every standard FT client. This is the most interoperable form.

Either way, update `FTDisplayClientTests` (the footer-bytes assertions) to lock in the corrected format, and add a case asserting the layer value is present and parseable in the emitted bytes.

## Notes

- Independent of this bug: to appear *in front* of demos, the app's layer must be higher than the demo layer (max 15). Once the layer is honored, a default of 5 will still be hidden if demos run on layers ≥ 5 — worth revisiting the default/guidance after this fix.
- The current code comment on `footerStart` ("allowing the receiver to distinguish it from PPM pixel data") is the root misconception — the server distinguishes the footer by length, so the marker byte is not only unnecessary but actively breaks parsing.

## Relation

- Pairs with: [#0047](0047.md) — added the layer UI/model; this bug means that layer value never actually reached the display.
- Pairs with: [#0050](0050.md) — continuous resend keeps a layer alive; both are needed for a layer to stay visible.

## Resolution notes

> 🟢 Resolved 2026-07-07 — The layer/offset now rides in the PPM header as `#FT: <x> <y> <z>` (the reference-client form) instead of a `0x00`-prefixed trailing footer, so the FT server actually applies the chosen layer. Pending hands-on confirmation on-network.

## Root cause

`FTDisplayClient.makePacket` appended the offset as a trailing footer beginning with a `0x00` byte (`<0x00><x>\n<y>\n<z>\n`). The FT server (`server/ppm-reader.cc`) parses trailing offsets with `skipWhitespace` (`isspace`) then `strtol`; `0x00` is neither whitespace nor a digit, so `readNextNumber` converts nothing and sets its cursor to `NULL`, which makes `parseOffsets` skip `offset_y` and `layer` entirely. Result: every packet's layer defaulted to 0 (background), regardless of the value chosen in #0047 — the image was composited behind any demo running on a higher layer.

## Fix

Replaced the trailing footer with the canonical PPM header comment: `makePacket` now injects `#FT: <x> <y> <z>` between the `<W> <H>` dimensions line and the `255` maxval, yielding `P6\n<W> <H>\n#FT: <x> <y> <z>\n255\n<pixels>` — byte-for-byte the form the reference client (`api/lib/udp-flaschen-taschen.cc`) emits and the server parses via its `#FT:` comment scan. A small `insertingFTComment(into:offset:)` helper does the insertion by locating the second header newline; the shared `VariantExporter` (used by file export) is untouched, so exported `.ppm` files are unchanged. No `0x00` byte is emitted.

## Review

Self-reviewed (single session). The change is confined to `makePacket`; the offset/layer value plumbing from #0047 is unchanged, so the UI still supplies 1–15. Verified the target format against three upstream sources (client, server parser, protocol doc) before implementing.

## Verification

`cd PixelArtGalleryKit && swift test` — 106 tests pass. `FTDisplayClientTests` was migrated to Swift Testing and rewritten for the new layout; key cases: `ftCommentEncodesOffsetAndLayer` asserts the packet starts with exactly `P6\n1 1\n#FT: 5 7 2\n255\n`, `packetContainsNoNulByte` asserts the `0x00` is gone (direct #0051 regression), and `pixelOrderingIsRowMajorRGB`/`packetByteCountMatchesHeaderPlusRGB` confirm the pixel payload is unchanged. `xcodebuild … -destination 'platform=macOS' build` — **BUILD SUCCEEDED**. End-to-end display confirmation is pending the user being back on the FT network.

## Files changed

- `PixelArtGalleryKit/Sources/PixelArtGalleryKit/Networking/FTDisplayClient.swift` — removed the `0x00` trailing footer; added `insertingFTComment(into:offset:)` and emit `#FT: x y z` in the PPM header.
- `PixelArtGalleryKit/Tests/PixelArtGalleryKitTests/Networking/FTDisplayClientTests.swift` — migrated to Swift Testing; assertions updated for the header-comment format plus the no-NUL regression test.

## Gotchas

- The FT server accepts offsets *both* as a `#FT:` header comment and as trailing numbers after the pixels — but the trailing form must be plain whitespace-delimited decimals. The original code's `0x00` marker byte (added on the assumption the server needed to distinguish the footer from pixel data — it uses byte count instead) is what broke it.
- To actually appear *in front* of on-display demos, the chosen layer must be higher than the demo's layer (max 15). This fix makes the layer take effect; picking a high enough value is a separate, user-facing concern (see #0047 notes).
